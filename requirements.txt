# 当前项目运行依赖。
#
# 推荐运行环境：
# - Docker 镜像：nvcr.io/nvidia/pytorch:21.05-py3
# - Python：使用基础镜像内置的 3.8
# - PyTorch / torchvision：由基础镜像提供，不在本文件中重复安装
#
# 本地开发：
# - 优先使用 Python 3.8。
# - Python 3.12 不是当前 YOLOv5 v6.0 项目的依赖基线。
# - 如果脱离 Docker 做 GPU 推理，请先按本机 CUDA 版本单独安装 torch/torchvision，
#   再安装本文件中的依赖。

# 核心数值计算 / 图像处理依赖 --------------------------------------------------
# numpy==1.20.3 和 Pillow==9.0.0 与 Docker 运行环境对齐。
# 除非同步更换基础镜像并完成回归验证，否则不要随意升级。
matplotlib>=3.2.2
numpy==1.20.3
opencv-python>=4.1.1
Pillow==9.0.0
scipy>=1.4.1

# 配置读取 / HTTP 请求 / 几何计算 ---------------------------------------------
PyYAML>=5.3.1
requests>=2.23.0
Shapely

# 兼容性固定 ------------------------------------------------------------------
# protobuf 4.x 可能导致旧版 TensorBoard / 训练工具异常，因此固定到 3.20.0。
protobuf==3.20.0

# PyTorch --------------------------------------------------------------------
# torch/torchvision 已由 NVIDIA PyTorch 基础镜像安装。
# torch>=1.7.0
# torchvision>=0.8.1

# 命令行进度条 ----------------------------------------------------------------
tqdm>=4.41.0

# 训练日志 --------------------------------------------------------------------
tensorboard>=2.4.1
# wandb

# 训练结果绘图 / 表格处理 ------------------------------------------------------
# YOLOv5 的结果表格、训练结果读取和 evolve 绘图逻辑会导入 pandas。
pandas>=1.1.4
seaborn>=0.11.0

# 模型导出可选依赖 ------------------------------------------------------------
# 以下依赖只在导出对应格式时安装，默认运行和训练不强制需要。
# coremltools>=4.1  # CoreML 导出
# onnx>=1.9.0  # ONNX 导出
# onnx-simplifier>=0.3.6  # ONNX 简化
# scikit-learn==0.19.2  # CoreML 量化
# tensorflow>=2.4.1  # TFLite 导出
# tensorflowjs>=3.9.0  # TF.js 导出

# 其他可选能力 ----------------------------------------------------------------
# albumentations>=1.0.3  # 额外数据增强
# Cython  # pycocotools 构建依赖，见 https://github.com/cocodataset/cocoapi/issues/172
# pycocotools>=2.0  # COCO mAP 评估
# roboflow  # 数据集管理/导出
thop  # FLOPs 计算
